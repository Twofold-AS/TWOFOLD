import { Hero } from "@blocks";

export default function Page() {
  return (
    <main>
      <Hero
        title="Build faster with Twofold"
        subtitle="Reusable blocks wired into your apps. This is the Hero block rendered from packages/blocks."
        ctaLabel="Get started"
        ctaHref="/#get-started"
      />
    </main>
  );
}